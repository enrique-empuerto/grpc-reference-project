# grpc-reference-project for myself

### 1. follow this workspace file
```
{
  "folders": [
    {
      "path": "grpc-server"
    },
    {
      "path": "grpc-client"
    }
  ],
  "settings": {},
}
```

### 2. use launch.json to define launch configurations for both projects

### 3. use task json to define build tasks 